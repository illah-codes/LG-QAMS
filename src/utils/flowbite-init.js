/**
 * Flowbite initialization utility
 * Centralized import to ensure proper bundling
 */
import { initFlowbite } from 'flowbite';

/**
 * Initialize Flowbite components
 */
export function initializeFlowbite() {
  initFlowbite();
}

/**
 * Re-export initFlowbite for direct use if needed
 */
export { initFlowbite };
